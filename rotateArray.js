// 
// 
// 
function waveMatrix(mat)
{
    for(let col=0;col<mat[i];col++)
    {
        if(col%2==0)
        {
        for(let row=0;row<arr.length;row++)
        {
            console.log(mat[row[col]])
        }}
        else
        {
            for(row=mat.length-1;row>=0;row--)
            {
                console.log(mat[row[col]])
            }
        }
    }
}
          let mat=[[1,2,3],
           [4,5,6],
           [7,8,9]]
waveMatrix(mat);
                                                 pol